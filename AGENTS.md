# AGENTS.md

## 项目概览

这是一个无需构建工具的静态前端待办清单应用，直接由 `index.html`、`styles.css` 和 `app.js` 组成。页面重点围绕“专注模式 / Focus mode”展开，提供一个视觉完成度较高的 Todo List 界面。

项目当前定位：

- 纯浏览器端运行
- 无后端
- 无打包工具
- 无第三方依赖
- 数据保存在浏览器 `localStorage`

从 Git 历史看，当前仓库只有一次初始化提交：

- `Initial todo list app`

## 文件结构

当前项目文件很少，职责清晰：

- `index.html`
  - 页面骨架
  - 定义 Hero 区、输入表单、筛选栏、任务列表、底部操作区
- `styles.css`
  - 全部视觉样式
  - 包含明暗主题、响应式布局、渐变背景、卡片与按钮样式
- `app.js`
  - 全部交互逻辑
  - 包含状态管理、国际化、主题切换、任务增删改查、筛选、持久化
- `.gitignore`
  - 忽略常见编辑器文件、日志、依赖目录、构建产物和环境变量文件

## 主要功能

项目已经实现的功能：

- 新增待办事项
- 标记任务完成 / 未完成
- 删除任务
- 编辑任务标题
- 编辑任务截止时间
- 按 `全部 / 未完成 / 已完成` 筛选
- 清除所有已完成任务
- 显示总数、未完成数、已完成数
- 设置任务截止时间
- 提供时间快捷按钮：
  - 现在
  - `+1` 小时
  - 明天 `9:00`
- 自动识别逾期任务并显示状态
- 中英双语切换
- 明暗主题切换
- 刷新页面后保留任务数据

## 技术实现

### 1. 页面结构

HTML 采用单页结构，核心区域分为两部分：

- `hero`
  - 标题、说明文案、主题切换按钮
- `content`
  - 新建任务表单
  - 统计信息
  - 筛选器
  - 任务列表
  - 空状态提示
  - 语言切换与清理按钮

### 2. 状态管理

`app.js` 使用非常轻量的原生 JavaScript 状态方案：

- `state`
  - 保存所有待办任务数组
- `filter`
  - 保存当前筛选条件，默认值为 `all`
- `language`
  - 保存当前语言，默认优先读取本地存储，否则为 `en`
- `theme`
  - 保存当前主题，默认优先读取本地存储，否则为 `dark`

状态更新后通过 `saveState()` 写回本地，再调用 `render()` 重新渲染界面。

### 3. 本地存储

项目使用以下 `localStorage` 键：

- `codex.todo-list`
  - 待办任务数组
- `codex.todo-language`
  - 当前语言
- `codex.todo-theme`
  - 当前主题

### 4. 数据结构

每条 Todo 的结构如下：

```js
{
  id: string,
  title: string,
  completed: boolean,
  createdAt: number,
  dueAt: number | null
}
```

说明：

- `id` 使用 `crypto.randomUUID()` 生成
- `createdAt` 和 `dueAt` 都是时间戳
- `dueAt` 为空时表示没有截止时间

### 5. 国际化

项目内置一套简单的 `i18n` 字典，支持：

- `en`
- `zh`

国际化覆盖了：

- 标题和说明文案
- 按钮文字
- 筛选标签
- 时间输入占位符
- 状态文案
- 主题与语言切换的无障碍文案

### 6. 主题系统

样式通过 CSS 变量组织，默认是深色主题，浅色主题通过给 `body` 添加 `theme-light` 类切换。

设计特点：

- 多层渐变背景
- 半透明卡片
- 较强的玻璃拟态氛围
- 边框和阴影用于建立层次
- 移动端适配已完成

## 运行方式

这是一个静态页面项目，通常有两种打开方式：

- 直接双击 `index.html` 用浏览器打开
- 用任意静态服务器在项目根目录启动后访问页面

因为没有发现 `package.json`，所以目前：

- 没有 npm 脚本
- 没有构建命令
- 没有测试命令
- 没有 lint 配置

## 当前代码风格与约定

从现有代码可以总结出一些隐含约定：

- 使用原生 HTML / CSS / JavaScript
- 变量与函数命名采用英文
- 常量使用全大写下划线命名
- DOM 查询集中放在文件开头
- 交互逻辑以事件监听方式组织
- 通过 `render()` 统一刷新界面
- 样式以组件块为单位分组书写

如果后续继续开发，最好延续这套轻量结构，避免无必要地引入复杂框架。

## 已知风险与注意点

### 1. 中文时间解析疑似存在问题

`app.js` 中 `parseTimeInputValue()` 的第二个正则看起来出现了乱码，原意应当是解析中文格式时间（例如 `YYYY年MM月DD日 HH:mm`），但当前代码大概率无法稳定匹配中文输入。

这意味着：

- 英文格式时间输入看起来是正常的
- 中文格式时间输入可能解析失败
- “编辑截止时间”时如果用户输入中文格式，可能触发无效时间提示

这是后续优先值得检查的一点。

### 2. 任务编辑依赖 `prompt()`

当前编辑任务标题和截止时间使用浏览器原生 `prompt()`，实现简单，但体验一般：

- 样式不可控
- 移动端体验较弱
- 无法做更细致的表单校验

如果后续要提升产品感，可以考虑改成自定义弹层或内联编辑。

### 3. 没有自动化测试

目前没有看到任何测试文件或测试工具，因此后续改动需要依赖手动验证。

建议重点手测：

- 新增任务
- 切换完成状态
- 筛选功能
- 清除已完成
- 语言切换
- 主题切换
- 带截止时间的新增与编辑
- 刷新后的本地持久化

## 后续协作建议

如果以后继续在这个仓库里开发，可以优先按下面的顺序思考：

1. 先修复中文时间解析问题
2. 再补一个更友好的编辑交互
3. 然后补充基础测试或最少的手工验证清单
4. 若功能继续增长，再考虑是否引入模块化拆分

## 一句话总结

这是一个完成度不错的原生前端 Todo 应用，亮点是本地持久化、双语切换、主题切换和截止时间管理；当前最值得关注的技术点是中文时间解析实现可能存在异常。
